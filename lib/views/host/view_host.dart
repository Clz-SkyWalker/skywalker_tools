import 'package:flutter/material.dart';

class ViewHost extends StatelessWidget {
  const ViewHost({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('host'));
  }
}
